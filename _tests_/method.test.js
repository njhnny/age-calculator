import Triangle from './src/method.js';

describe('Method', () => {
  
    test('should multiply user age by planet multipliers', () => {
      const method = new Method(28, []);
      //expectmethod;
      
    });
});