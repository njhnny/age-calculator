export default class Method {
  constructor() {
    this.age = age;
    this.spaceAges = [];
  }

  ageCalculator() {
    let ageMultipliers = [.24, .62, 1.88, 11.86];
    let variable = this.spaceAges;
    let age2 = age;
    ageMultipliers.forEach(myFunction)
    function myFunction(multiplier) {
      variable.push(age2*multiplier);
  }
  return this.spaceAges;
  }
}